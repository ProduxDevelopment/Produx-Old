/**
 * This redirects the user to a 2FA screen where they have to check their emails for a message.
 */

import { Router } from "express";
const router = Router()

/*
router.get("/", (req, res => {

})
*/ 

export default router;
